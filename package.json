{
  "name": "fraldacycle",
  "private": true,
  "version": "0.1.1",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write .",
    "verify:demo": "node --check apps/web/src/app.js && node --check apps/web/src/map.js && node --check apps/web/src/notifications.js && node --check apps/web/src/service-worker.js && node --test apps/web/src/demo-api.test.js apps/web/src/server.test.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.3.0"
  }
}
