# Application
NODE_ENV=development
PORT=3000

# Storage
LISTINGS_DATA_FILE=data/listings.json
USERS_DATA_FILE=data/users.json
NOTIFICATIONS_DATA_FILE=data/notifications.json

# Authentication
# Use a unique value with at least 32 characters outside development.
AUTH_SECRET=replace-with-a-unique-secret-of-at-least-32-characters

# Moderation
# Comma-separated e-mails permitted to approve, close, or block listings.
MODERATOR_EMAILS=

# Database
DATABASE_URL=
